#include<vector>
#include<string>

using std::vector;

vector<double> mergeSolutionPath(vector<vector<double> > solPath);
void dumpSolutionPath(vector<vector<double> > solPath, std::string outFilename);
